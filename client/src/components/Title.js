import React from "react";
import styles from "../styles/SensorTable.module.css";

const Title = () => {
  return <span className={`${styles.title}`}>Security Alarm System</span>;
};

export default Title;
