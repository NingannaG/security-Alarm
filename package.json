{
  "name": "security-alarm-system",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start-client": "cd client && start npm start",
    "start-server": "cd server && start npm start",
    "start-app": "concurrently \"npm run start-client\" \"npm run start-server\"",
    "install-app": "concurrently \"npm install --prefix client\" \"npm install --prefix server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
